import { HeroSection } from "@components/Hero";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};

export default Home;
